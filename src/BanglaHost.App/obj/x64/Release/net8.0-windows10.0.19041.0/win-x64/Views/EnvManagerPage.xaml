<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BanglaHost.App.Views.EnvManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Environment Variable Manager" FontSize="26" FontWeight="SemiBold" Grid.Row="0"/>

        <Border Grid.Row="1" BorderThickness="1" CornerRadius="8" Padding="12"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBlock Text="Select Site:" VerticalAlignment="Center"/>
                <ComboBox x:ConnectionId='3' x:Name="SiteBox" Width="200"                                />
                <Button x:ConnectionId='4' Content="Save .env"                    Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" BorderThickness="1" CornerRadius="8" Padding="4"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <TextBox x:ConnectionId='2' x:Name="EnvEditor" AcceptsReturn="True" FontFamily="Consolas" FontSize="14"
                     Header=".env Content" Padding="12"/>
        </Border>
    </Grid>
</Page>

