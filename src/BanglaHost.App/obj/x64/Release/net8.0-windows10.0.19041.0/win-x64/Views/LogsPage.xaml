<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BanglaHost.App.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:ConnectionId='2' x:Name="PageTitle" Grid.Row="0" Text="Logs" FontSize="26" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
            <ComboBox x:ConnectionId='5' x:Name="FilePicker" Width="280"                                />
            <Button x:ConnectionId='6' x:Name="RefreshBtn" Content="Refresh"                      />
        </StackPanel>

        <Border Grid.Row="2" BorderThickness="1" CornerRadius="8" Padding="12"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <ScrollViewer x:ConnectionId='3' x:Name="Scroll">
                <TextBlock x:ConnectionId='4' x:Name="LogText" FontFamily="Consolas" FontSize="12" TextWrapping="NoWrap" IsTextSelectionEnabled="True"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>

