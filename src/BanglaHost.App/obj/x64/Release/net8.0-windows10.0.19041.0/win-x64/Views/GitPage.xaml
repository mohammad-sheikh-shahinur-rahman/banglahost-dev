<?xml version="1.0" encoding="utf-8"?>
<Page x:ConnectionId='1'
    x:Class="BanglaHost.App.Views.GitPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        >

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel Spacing="4">
                <TextBlock Text="Git Manager" FontSize="26" FontWeight="SemiBold"/>
                <TextBlock Opacity="0.7" Text="Monitor git status and branches across all your sites."/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button x:ConnectionId='9' Content="GitHub Login"                          />
                <Button x:ConnectionId='10' Content="Git Clone"                        Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" BorderThickness="1" CornerRadius="8" Padding="4"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <ListView x:ConnectionId='2' x:Name="GitList" SelectionMode="None">
                <ListView.Header>
                    <Grid Padding="12,8" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Site Name" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="Branch" FontWeight="Bold"/>
                        <TextBlock Grid.Column="2" Text="Status" FontWeight="Bold"/>
                        <TextBlock Grid.Column="3" Text="Actions" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,0,12,0"/>
                    </Grid>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="12,8" ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Branch}" VerticalAlignment="Center" Opacity="0.8"/>
                            <TextBlock Grid.Column="2" Text="{Binding Status}" VerticalAlignment="Center" Foreground="{Binding StatusColor}"/>

                            <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="4">
                                <Button x:ConnectionId='4' Content="Pull"                    Tag="{Binding Name}"/>
                                <Button x:ConnectionId='5' Content="Push"                    Tag="{Binding Name}"/>
                                <Button x:ConnectionId='6' Content="Commit"                      Tag="{Binding Name}"/>
                                <Button x:ConnectionId='7' Content="Branch"                      Tag="{Binding Name}"/>
                                <Button x:ConnectionId='8'                        Tag="{Binding Root}">
                                    <FontIcon Glyph="&#xE756;" FontSize="14"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>

