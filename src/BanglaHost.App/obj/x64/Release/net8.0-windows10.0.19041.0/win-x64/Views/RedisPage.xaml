<?xml version="1.0" encoding="utf-8"?>
<Page x:ConnectionId='1'
    x:Class="BanglaHost.App.Views.RedisPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        >

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Redis Manager" FontSize="26" FontWeight="SemiBold"/>
            <TextBlock Opacity="0.7" Text="Inspect keys, refresh stats, and clean up the local Redis database."/>
        </StackPanel>

        <Border Grid.Row="1" BorderThickness="1" CornerRadius="8" Padding="12"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Spacing="10">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button x:ConnectionId='6' Content="Refresh"                       Style="{StaticResource AccentButtonStyle}"/>
                    <Button x:ConnectionId='7' Content="Delete Selected"                     />
                    <Button x:ConnectionId='8' Content="Flush DB"                    />
                    <TextBlock x:ConnectionId='9' x:Name="RedisStatus" VerticalAlignment="Center" Text="Checking..."/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBox x:ConnectionId='4' x:Name="PatternBox" Width="260" Header="Key Pattern" Text="*" PlaceholderText="* or cache:*"/>
                    <TextBlock x:ConnectionId='5' x:Name="SelectedKeyText" VerticalAlignment="Bottom" Opacity="0.7" Text="No key selected"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderThickness="1" CornerRadius="8" Padding="12"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Recent Keys" FontWeight="SemiBold"/>
                    <ListView x:ConnectionId='3' x:Name="KeysList" Height="400"                                              SelectionMode="Single"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" BorderThickness="1" CornerRadius="8" Padding="12"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <ScrollViewer>
                    <StackPanel Spacing="8">
                        <TextBlock Text="Redis Info / Stats" FontWeight="SemiBold"/>
                        <TextBlock x:ConnectionId='2' x:Name="InfoBlock" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>

