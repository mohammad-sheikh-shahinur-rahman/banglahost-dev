<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BanglaHost.App.Views.ApiClientPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="API Client" FontSize="26" FontWeight="SemiBold" Grid.Row="0"/>

        <Border Grid.Row="1" BorderThickness="1" CornerRadius="8" Padding="12"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid ColumnSpacing="8" RowSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ComboBox x:ConnectionId='4' x:Name="MethodBox" Grid.Column="0" HorizontalAlignment="Stretch"                                              >
                    <ComboBoxItem Content="GET" IsSelected="True"/>
                    <ComboBoxItem Content="POST"/>
                    <ComboBoxItem Content="PUT"/>
                    <ComboBoxItem Content="DELETE"/>
                    <ComboBoxItem Content="PATCH"/>
                    <ComboBoxItem Content="GRAPHQL"/>
                    <ComboBoxItem Content="WEBSOCKET"/>
                </ComboBox>
                <TextBox x:ConnectionId='5' x:Name="UrlBox" Grid.Column="1" PlaceholderText="http://mysite.test/api/user" Text="http://"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                    <Button x:ConnectionId='7' x:Name="SendBtn" Content="Send Request" Style="{StaticResource AccentButtonStyle}"                   />
                    <Button x:ConnectionId='8' x:Name="ConnectBtn" Content="Connect"                       Visibility="Collapsed"/>
                    <Button x:ConnectionId='9' x:Name="DisconnectBtn" Content="Disconnect"                          Visibility="Collapsed"/>
                </StackPanel>

                <TextBox x:ConnectionId='6' x:Name="BodyBox" Grid.Row="1" Grid.ColumnSpan="2" Header="Request Body"
                         AcceptsReturn="True" Height="100" FontFamily="Consolas"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="8">
                <TextBlock Text="Headers" FontWeight="SemiBold"/>
                <Border BorderThickness="1" CornerRadius="8" Padding="12"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <ScrollViewer Height="400">
                        <TextBlock x:ConnectionId='3' x:Name="HeadersBlock" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Spacing="8">
                <TextBlock Text="Response" FontWeight="SemiBold"/>
                <Border BorderThickness="1" CornerRadius="8" Padding="12"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <ScrollViewer Height="400">
                        <TextBlock x:ConnectionId='2' x:Name="ResponseBlock" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Page>

