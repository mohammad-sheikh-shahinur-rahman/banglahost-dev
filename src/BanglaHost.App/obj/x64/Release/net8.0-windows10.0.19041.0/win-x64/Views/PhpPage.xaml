<?xml version="1.0" encoding="utf-8"?>
<Page x:ConnectionId='1'
    x:Class="BanglaHost.App.Views.PhpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BanglaHost.App.Views"
                        >
    
    <ScrollViewer>
    <StackPanel Padding="24" Spacing="16">
        <StackPanel Spacing="2">
            <TextBlock Text="PHP Tools" FontSize="26" FontWeight="SemiBold"/>
            <TextBlock Opacity="0.7" Text="Manage Xdebug, php.ini, and logs for each installed PHP version."/>
        </StackPanel>

        <TextBlock Text="Installed PHP Versions" FontSize="16" FontWeight="SemiBold"/>
        <Border BorderThickness="1" CornerRadius="8" Padding="4"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel>
                <TextBlock x:ConnectionId='5' x:Name="EmptyVers" Text="No PHP versions installed yet." Opacity="0.6" Padding="10" Visibility="Collapsed"/>
                <ListView x:ConnectionId='6' x:Name="VersList" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate                              >
                            <Grid x:ConnectionId='8' Padding="8,6" ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:ConnectionId='9' Grid.Column="0"                         FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <TextBlock x:ConnectionId='10' Grid.Column="1"                            Opacity="0.7" VerticalAlignment="Center"                                  />
                                
                                <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="6">
                                    <ToggleSwitch x:ConnectionId='11' OffContent="Xdebug Off" OnContent="Xdebug On"                                                                                         Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <Button x:ConnectionId='12'                                                                                                                     ToolTipService.ToolTip="Download and enable ionCube Loader"/>
                                    <Button x:ConnectionId='13' Content="Extensions"                                                />
                                    <Button x:ConnectionId='14' Content="Edit php.ini"                                             />
                                    <Button x:ConnectionId='15' Content="Error Log"                                              />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>

        <TextBlock Text="Composer" FontSize="16" FontWeight="SemiBold" Margin="0,16,0,0"/>
        <Border BorderThickness="1" CornerRadius="8" Padding="16"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button x:ConnectionId='2' Content="Update Composer"                             />
                <ProgressRing x:ConnectionId='3' x:Name="ComposerBusy" IsActive="False" Width="20" Height="20" VerticalAlignment="Center"/>
                <TextBlock x:ConnectionId='4' x:Name="ComposerStatus" VerticalAlignment="Center" Opacity="0.8"/>
            </StackPanel>
        </Border>
    </StackPanel>
    </ScrollViewer>
</Page>

