<Project Sdk="Microsoft.NET.Sdk">
  <!-- Tiny privileged helper. requireAdministrator manifest → one UAC prompt does the
       few operations that actually need admin: hosts-file edits + mkcert CA install.
       The main banglahost.exe stays unprivileged and shells out to this for those verbs. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>BanglaHost.Elevate</RootNamespace>
    <AssemblyName>banglahost-elevate</AssemblyName>
    <ApplicationManifest>app.manifest</ApplicationManifest>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\BanglaHost.Core\BanglaHost.Core.csproj" />
  </ItemGroup>
</Project>
